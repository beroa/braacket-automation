const challonge = require("./apis/challonge");
const braacket = require("./apis/braacket");

async function main() {
  // let ids = await challonge.get_tournaments("pm");
  // console.log("ids", ids);
  // await braacket.upload_challonge(ids, null, process.env.BRAACKET_PM_LEAGUE);
  // await braacket.upload_challonge(ids, null, process.env.BRAACKET_MELEE_LEAGUE);
  // await challonge.mark_uploaded(ids);
}

main();
